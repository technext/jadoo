mixin SectionTitle(subtitle, title, location='center')
  h5(class=`text-secondary text-${location}`) #{subtitle} 
  h3(class=`fs-xl-10 fs-lg-8 fs-7 fw-bold font-cursive text-${location} text-capitalize`)= title
